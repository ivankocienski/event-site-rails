# Search

Open Search.

There is a `.env` file that defines `OPENSEARCH_INITIAL_ADMIN_PASSWORD` - This is used when first run to provision the servers and configure them.

Run with `docker compose up`- it connects to the current terminal so CTRL-C will stop the containers. Run `docker compose down` to clean up the containers.

The data in the index persists.


